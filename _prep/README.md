# 準備メモ（本番接続済み）

時事クイズは **本番接続完了**。実体はゲーム直下:

- `../quizNewsData.js` … 60問パック
- `../quiz-data.json` … 同内容のJSON
- `../quiz-engine.js` … 抽選エンジン
- `../main.js` … ミミック種別×難易度で出題

小学向け旧データは `../_unused/quizData.js` に退避。
